'use strict';

/**
 * receta-ingrediente controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::receta-ingrediente.receta-ingrediente');
