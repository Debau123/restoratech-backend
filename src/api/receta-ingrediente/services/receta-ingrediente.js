'use strict';

/**
 * receta-ingrediente service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::receta-ingrediente.receta-ingrediente');
