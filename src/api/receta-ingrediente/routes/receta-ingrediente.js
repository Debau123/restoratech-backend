'use strict';

/**
 * receta-ingrediente router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::receta-ingrediente.receta-ingrediente');
